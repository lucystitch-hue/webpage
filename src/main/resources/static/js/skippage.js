//window.addEventListener('popstate', function (event) {
//      // Here, you can specify the URL of the page you want to redirect to
//      const desiredPageUrl = '/WEB/notification/notification-management';
//
//      // Redirect the user to the desired page
//      window.location.href = desiredPageUrl;
//    });
//
//    // Push a new state to the history to override the default "Back" behavior
//    window.history.pushState(null, null, null);